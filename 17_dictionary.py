# 순서 번호 대신 이름표로 값을 바로 찾는 자료 구조
# 센서 이름처럼 이름이 분명한 데이터에 가장 적합

# ==================================================

# 리스트의 불편함과 딕셔너리의 등장
# 위치 관리의 부담을 키, 값 짝지음으로 풀어낸다.
# -------------------------------------------------

# 01. 위치 관리
#   리스트의 한계
#   센서가 빠지면 모든 번호가 밀려 다시 외워야함

# 20개 센서 중 7번을 떼면 8번부터 전부 한 칸씩 밀린다.
# 외워둔 위치가 모두 어긋난다.

# -------------------------------------------------

# 02. 사전 비유
#   딕셔너리의 직관
#   단어를 찾으면 뜻이 나오듯 키로 값을 찾음


# -------------------------------------------------

# 03. 키와 값
#   핵심 구조
#   이름표는 키, 연결된 값은 밸류로 짝지어 저장


# ==================================================

# 중괄호 안에 키와 값을 콜론(:)으로 짝지어 생성.

# 문법
# {키 : 값, 키 : 값}
# 왼쪽이 이름표(키), 오른쪽이 그 값.
# 여러 쌍은 쉼표(,)로 구분.
# 키는 보통 문자열, 값에는 거의 무엇이든 가능.

# -------------------------------------------------

# 셋과의 구분
# 콜론 유무로 셋과 구분

# - 콜론(:) 있으면 딕셔너리
# - 빈 중괄호{}는 딕셔너리

# 빈 셋은 set() 함수로 만들어야 한다.

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- 딕셔너리 만들기 - 코드로 확인 --")
print("\n")

# 리스트로 이름 나열
data_class = ["A", "B", "C"]
# 키와 값을 콜론으로 짝지어 생성

# 딕셔너리로 정확하게 역할까지 부여
data_class_dict = {"반장": "A", "부반장": "B ", "당번": "C"}


# 센서로 부터 얻는 예시 데이터로 딕셔너리를 만들기
sensors = {"센서이름": "보일러", "모터온도": 78, "진동": 0.5}
print(sensors)
print(type(sensors))  # <class 'dict'>
emty = {}  # 빈 딕셔너리 생성

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- 키로 값 꺼내기 --")
print("\n")

#  -------------------------------------------------
# 대괄호 안에 키를 넣어 연결된 값을 바로 꺼낸다.

# 문법
# 딕셔너리 [키]

# sensors = {"센서이름": "보일러", "모터온도": 78, "진동": 0.5}
print(sensors["센서이름"])
print(sensors["모터온도"])
print(sensors["진동"])

#  -------------------------------------------------
# 위치를 몰라도 이름만 알면 됨
# 센서 100개여도 이름으로 즉시 조회
# 없는 키를 꺼내면 KeyError

# 안전한 조회는 잠시 뒤 get으로 해결

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- 새로운 키-값 추가하기 --")
print("\n")
# 없는 키에 값을 할당하면 그 쌍이 새로 추가된다.

#  -------------------------------------------------
# 문법
# 딕셔너리[새 키] = 값

# 리스트의 append 같은 별도 명령이 필요 없다.
# 빈 딕셔너리부터 시작해 데이터가 들어올 때마다 쌓는다.
# 같은 문법이 추가와 수정 모두 처리.

#  -------------------------------------------------
# 별도 명령 없이 이름표만 붙이면 됨
# - 빈 딕셔너리부터 하나씩 쌓기 가능
# - 같은 문법이 추가와 수정 모두 처리

print("\n")
# ==================================================
print("==================================================")
print("\n")

# 기존에 있던 key의 값을 변경
sensors = {"센서이름": "보일러", "모터온도": 78}
sensors["센서이름"] = "펌프"  # 센서이름 값 변경
print(sensors)  # {'센서이름': '펌프', '모터온도': 78}

# 기존에 없던 key의 값을 추가
sensors["펌프압력"] = 95
print(sensors)  # {'센서이름': '펌프', '모터온도': 78, '펌프압력': 95}
sensors["유량"] = 42
print(sensors)  # {'센서이름': '펌프', '모터온도': 78, '펌프압력': 95, '유량': 42}

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- 기존 값 수정하고 삭제하기 --")
print("\n")
# 있는 키에 값을 넣으면 수정, del로 키-값 쌍을 삭제.

#  -------------------------------------------------
# 수정 문법
# 딕셔너리[있는 키] = 새 값

# 삭제 문법
# del 딕셔너리[키]

# 추가할 때와 문법이 똑같다.
# 키 있으면 새로 만드는 대신 값만 바뀐다.
# 없는 키를 지우려 하면 오류 발생.

#  -------------------------------------------------
# 추가와 수정은 문법이 동일
# - 같은 이름표에 최신값 덮어쓰기
# - 삭제는 del 사용

# 측정할 때마다 값이 바뀌는 센서값은 항상 최신 상태 유지가 필요.

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- 수정, 삭제-코드로 확인 --")
print("\n")
# 값 수정과 키,값 삭제

# 더 이상 필요없는 key와 그 value 삭제
sensors = {"센서이름": "펌프", "모터온도": 78, "펌프압력": 95, "유량": 42}
del sensors["펌프압력"]
print(sensors)  # {'센서이름': '펌프', '모터온도': 78, '유량': 42}
del sensors["모터온도"]
print(sensors)  # {'센서이름': '펌프', '유량': 42}

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- get으로 없는 키 안전하게 다루기 --")
print("\n")
# get은 없는 키를 꺼내도 오류 대신 기본값을 돌려준다.

#  -------------------------------------------------
# 문법
# 딕셔너리.get(키, 기본값)

# 키 있으면 값, 없으면 오류 대신 None.
# 두 번째 인자로 기본값을 지정 가능.
# 여러 설비의 값을 더할 때 바진 센서를 0처리.

#  -------------------------------------------------
# 데이터가 들쭉날쭉할 때 안전장치
# - 키 없으면 None 또는 지정한 기본값
# - 일부 센서가 빠져도 계산 안 멈춤

# 어떤 설비엔 유량 센서가 있고 어떤 설비엔 없는 실무 상황의 안전장치

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- get-코드로 확인 --")
print("\n")
# 없는 키도 기본값으로 안전 처리.

print(sensors.get("센서이름"))  # 펌프
print(sensors.get("모터온도"))  # None

# moter_degree에는 숫자가 담길거라 생각했는 데...
moter_degree = sensors.get("모터온도", 0)

# 숫자가 안담기면 에러 발생
next_degree = moter_degree + 10
print(next_degree)

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- in으로 키 존재 확인하기 --")
print("\n")
# in연산자로 특정 키가 딕셔너리에 있는지 확인.

#  -------------------------------------------------
# 문법
# 키 in 딕셔너리

# 있으면 True, 없으면 False.
# 조건문과 함께 쓰면 꺼내거나 삭제 전 안전성 확보.
# get은 안전하게 꺼낼 때, in은 있는지만 확인할 때.

is_motor_degree_key = "모터온도" in sensors
print(is_motor_degree_key)

if is_motor_degree_key:
    print("그런 키 있어요!")
else:
    print("그런 키 없어요!")

    # 위 코드는 이렇게 보통 쓰인다

if "모터온도" in sensors:
    print("그런 키 있어요!")
else:
    print("그런 키 없어요!")


#  -------------------------------------------------
# 딕셔너리에서 in은 값이 아닌 키를 봄
# - 키 있으면 True, 없으면 False
# - 꺼내기, 삭제 전 확인으로 오류 방지

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- 딕셔너리 키가 될 수 있는 것 --")
print("\n")
# 바뀌지 않는 값만 키가 될 수 있다 - 고정된 주소처럼 쓰이기 떄문.

# (자료형 - 키 가능 - 이유)
# 문자열 - 가능 - 안 바뀜 : "모터온도"같이 한 번 정해지면 변경 불가
# 숫자 - 가능 - 안바뀜 : 정수,실수 모두 키로 사용 가능
# 튜플 - 가능 - 안바뀜 : 앞서 튜플이 키 될 수 있던 이유
# 리스트 - 불가 - 바뀜 : 이사 간 집으로 편지를 못 보내는 것과 같음


print("\n")
# ==================================================
print("==================================================")
print("\n")
print("== 순회와 갱신 ==")

print("\n")
# ==================================================
print("==================================================")
print("\n")

print("-- keys와 values로 키,값만 꺼내기 --")
print("\n")
# kwy는 모든 이름표를, values는 모든 값을 한 번에 모은다.

#  -------------------------------------------------
# 문법
# 딕셔너리.keys()
# 딕셔너리.values()

# 한 쪽 묶음만 뗴어내 다룰 때 사용.
# for로 하나씩 꺼낼 수 있고 list로 감싸 리스트로도 변환 가능.
# keys로 꺼낸 이름을 셋으로 바꿔 비교에도 활용

#  -------------------------------------------------
# 한 쪽 묶음만 뗴어내 다룰 때 사용
# - 센서 이름 목록은 keys
# - 측정값 합계,평균은 values

# values로 sum과 len 조합 -> 평균, keys로 정렬도 가능.

#  -------------------------------------------------
# keys를 가져와봅시다
print(sensors.keys())

# values를 가져와봅시다
print(sensors.values())

# len을 통해 몇개의 key들이 있는지 살펴봅시다
print(len(sensors))


print("\n")
# ==================================================
print("==================================================")
print("\n")
