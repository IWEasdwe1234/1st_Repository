# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
ln = f"\n{"~" * 30}\n"
print(ln)

# 실습 4. 삭제 손실 비교
print("실습 4. 삭제 손실 비교")

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
print(ln)

# 삭제 방식별 남는 행 수와 손실률을 표로 비교

import pandas as pd

df = pd.read_csv("data/15_02_사출성형_공정.csv", encoding="utf-8")


# 목표
# 삭제 방식별 남는 행 수와 손실률을 표로 비교


# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

# 단계
# 원본·행삭제·thresh 각 방식의 남는 행 수 구하기
print("==[ 원본·행삭제·thresh 각 방식의 남는 행 수 구하기 ]==\n")
비교 = pd.DataFrame(
    {
        "방식": ["원본", "행삭제", "thresh20"],
        "행": [len(df), len(df.dropna()), len(df.dropna(thresh=20))],
    }
)
print(비교)

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
print(ln)

# 방식과 행 수를 하나의 표로 모으기
print("==[ 방식과 행 수를 하나의 표로 모으기 ]==\n")

비교["손실률"] = ((1 - (비교["행"] / len(df))) * 100).round(2)
print(비교)

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
print(ln)

# 원본 대비 손실률을 백분율로 계산해 나란히 보기
print("==[  ]==\n")


# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
print(ln)
