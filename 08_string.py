# """ """ - 여러 줄 문자열

notice = """설비 점검 안내
1. 전원 확인
2. 센서 점검"""

print(notice)
# 설비 점검 안내
# 1. 전원 확인
# 2. 센서 점검
# 위와 같이 직접 작성한 줄바꿈이 반영되어 여러 줄로 출력함

# 작성하는 개발자가 보기 편한 방식으로 출력했을 때 문제
notice = """
설비 점검 안내
1. 전원 확인
2. 센서 점검
"""

print(notice)
#
# 설비 점검 안내
# 1. 전원 확인
# 2. 센서 점검
#
# 개발자가 보기 편한 방식으로 작성하면 생각과 다른 결과물이 나옴
# """ """ (삼중 따옴표를 사용할 시 그 내부의 모든 줄바꿈이 다 반영되어 출력)

# 탭
notice = """설비 점검 안내
    1. 전원 확인
2. 센서 점검"""

print(notice)
# 삼중 따옴표는 탭도 그대로 유지됨

# =======================
# 이스케이프 문자

# notice = "설비 점검 안내\n1. 전원 확인\n2. 센서 점검"
print(notice)

tap = "이름\t상태"
print(tap)
print("이름 상태")

backslash = "이름\\상태"
print(backslash)  # 이름\상태 > 첫 번째 \는 이스케이프 문자라는 것을 알리는 용도

quotes = "It's me"  # 감싸는 따옴표와 str 내부 따옴표의 종류가 같을 때는 \를 사용
print(quotes)

# 빈 문자열과 공백 문자열의 차이
# "" 따옴표러 감싸졌지만 아무것도 작성되지 않았다면 "빈 문자열"
# 빈 문자열은 글자수 0, 길이 0
# " " 따옴표 안에 공백(스페이스바)이 있는 경우는 "공백 문자열"
# 공백(스페이스바)의 수 만큼 글자가 있고, 길이가 세어짐
# 빈 문자열과 공백 문자열은 컴퓨터에게 다른 값으로 인식됨
print("" == " ")  # False

# ========================================
print("=== 실습 - 설비 정보 출력 카드 만들기 ===")
# 목표 : 여러 문자열 숫자 정보를 모아 여러 줄 정보 카드로 출력
# 단계 :
# 코드(int) 상태 가동시간 점검일을 변수로 각자 선언
# -> 한 번의 출력으로 여러 줄의 문자열이 보이도록 구성
# 예상결과 : 설비/상태/가동/점검 4줄 카드 출력
# 설비: PUMP_A       > str
# 상태: 정상         > str
# 가동: 1200         > !! int !!
# 점검: 2026-07-16   > str
code = "PUMP_A"
state = "정상"
hours = 1200
day = "2026-07-16"
card = "설비: " + code + "\n상태" + state + "\n가동" + str(hours) + "시간\n점검: " + day
print(card)

# ===================================
# 인덱싱 - 위치 번호로 글자를 하나 꺼내기
# 문자열[인덱스번호]
# 문자열의 첫 글자 인덱스는 0
print("=== 인덱싱 ===")

word = "PYTHON"
print(word[0], word[3], word[5])  # P H N

abc = "abcdefghijklmnopqrstuvwxyz"

# 자기 이름 출력하기 (성 빼고)
print(abc[19], abc[0], abc[4], abc[6], abc[20])

# print(word[100]) # IndexError
# word 변수에 저장된 문자열의 길이보다 큰 인덱스를 호출했기 때문

# 음수 인덱스
# 뒤에서 부터 -1

# ========================================
print("=== 슬라이싱 ===")

# 슬라이싱: 여러 글자를 구간으로 잘라내기
# 대괄호 안에 시작:끝 형태로 범위 지정 ex) word[1:4]는 1,2,3번 자리 (시작포함 끝 제외)
word = "PYTHON"
print(word[1:4])  # YTH
print(word[0:3])  # PYT
code = "EQP-001"
print(code[0:3])  # EQP (앞 세 글자)
print(code[4:7])  # 001 (뒤 세 글자)
# 슬라이싱은 end가 포함되지 않고 출력하기 때문에 없는 인덱스인 6도 사용할 수 있음

# print(word[6])  # 인덱싱은 정확하게 마지막 인덱스까지만 쓸 수 있고, 넘치면 Error

# 슬라이싱 - start 생략
# 처음부터 특정 인덱스까지 구간을 뽑아내고 싶을 때 사용

print(word[0:3])  # print(word[:3]) 동일한 동작

# 슬라이싱 - end 생략
# 특정 인덱스부터 끝까지 구간을 뽑아내고 싶을 때 사용
print(word[2:])  # 2번 인덱스부터 끝까지 출력
# print(word[2:6])과 동일한 동작

# 슬라이싱 - 전체 생략
print(word[:])  # print(word[0:6])와 동일한 동작
# :을 사용하고 start와 end를 모두 생략하면 모든 인덱스의 구간을 뽑아냄

# 슬라이싱 - 음수 인덱스 사용
print(word[-3:])  # HON
# 음수 인덱스 작성 시 그냥 그 인덱스부터 정방향으로 출력함
print(word[:-1])  # PYTHO
# 처음부터 -1(5)를 제외한 구간을 뽑아냄
# 역순 아님 주의
# 음수 인덱스 사용 시 컴퓨터가 알아서 정수 인덱스 찾아 치환해서 동작

# step으로 건너뛰기
# 문자열[시작:끝:간격(step)]
print(word[0:6:2])  # PTO
# PYTHON에서 첫 번째 글자는 명시했으니 거기서부터 출력
# step이 2이기 때문에 Y 뛰고, T (두 번째 점피) 출력
# H 뛰고, O (두 번째 점프) 출력
# N 뛰고 끝
# 두 글자를 뛰는게 아니라 두번 뛰는 것 (뛴 그 자리 글자를 출력한다)

print(word[0:6:1])  # PYTHON

# start와 end를 생략하고 step만 입력
print(word[::2])  # PTO
# word 변수의 모든 글자를 두 칸씩 뛰면서 출력

# 순서 뒤집기
print(word[::-1])  # NOHTYP
# step은 인덱스가 아니고, 음수 임력 시 문자열의 순서를 뒤집음

# 슬라이싱은 범위를 벗어나도 오류가 발생하지 않음
print("범위를 벗어난 슬라이싱", word[0:100])  # PYTHON을 정상 출력

# ================================
print("=== 실습 - start 생략 ===")
# 목표 : start생략[:n]으로 앞부분 가져오기
# 단계 :
# 1. 'temp_sensor' 저장
# 2. 4글자만 추출하기
word = "temp_sensor"
print(word[:4])

# ================================
print("=== 실습 - end 생략 ===")
# 목표 : end생략[n:]으로 뒤쪽 전체 가져오기
# 단계 :
# 1. 'temp_sensor' 저장
# 2. 5번부터 추출하기
word = "temp_sensor"
print(word[5:])

# ================================
print("=== 실습 [-n:] 음수 슬라이싱(뒤n글자) ===")
# 목표 : 음수 인덱스 슬라이싱[-n:]으로 뒤 n글자 가져오기
# 단계 :
# 1. 'sensor_01' 저장
# 2. 뒤 2글자 출력
word = "sensor_01"
print(word[-2:])

# ================================
print("=== 실습 step으로 건너뛰기 ===")
# 목표 : step[::2]으로 한 칸씩 건너뛰며 가져오기
# 단계 :
# 1. 'PYTHON' 저장
# 2. 두 칸씩 건너뛴 글자만 출력
word = "PYTHON"
print(word[::2])

# ================================
print("=== 실습 문자열 뒤집기 ===")
# 목표 : step 음수로 문자열을 거꾸로 뒤집기
# 단계 :
# 1. 'PYTHON' 저장
# 2. 저장한 문자열을 뒤집어 출력
word = "PYTHON"
print(word[::-1])

# 인덱싱 vs 슬라이싱
# : 없으면 -> 인덱싱
# : 있으면 -> 슬라이싱

# =====================================
print("=== len() 활용 ===")
# len() - 문자열의 길이 반환
# len(문자열)
print(len("Hello World!"))  # 12 (공백도 모두 글자 취급)
print(len(""))  # 0 (빈 문자열은 0 출력)

var = "여러분~! 한 시간만 더 하면 됩니다! 조금만 더 힘을 내주세요!"
print(len(var))  # 변수에 담긴 문자열의 길이 출력도 가능

print(len("이것도") - len("가능할까?"))
# len()은 int를 반환하기 때문에 연산 가능

print("abc길이: ", len(abc), "/마지막 인덱스 번호:", len(abc) - 1)

# 음수 인덱스를 사용하지 않고 마지막 인덱스 문자를 뽑고 싶을 때
print(abc[len(abc) - 1])

# ======================================
print("=== 실습 len()으로 길이 재기 ===")
# 목표 : lenn()으로 문자열의 길이(자릿수)를 확인
# 단계 :
# 전화번호 '01012345678'을 변수에 저장
# len()으로 자릿수 출력

num1 = "01012345678"
print(len(num1))

# =====================================
print("=== in 활용 ===")

# in - 특정 문자가 문자열에 포함되었는지 여부 확인
# "여부"를 확인하기 때문에 True 또는 False (bool)으로 결과 반환
# 찾을 문자열 in 문자열
print("고장" in "설비 고장 발생")  # True
print("정상" in "설비 고장 발생")  # False
print("설비에서 고장" in "설비 고장 발생")  # False
print("설비에서 고장" in "설비에서 고장이 났습니다.")  # True

# not in - in의 정반대 동작
print("고장" not in "설비 고장 발생")  # False
print("정상" not in "설비 고장 발생")  # True
print("설비에서 고장" not in "설비 고장 발생")  # True
print("설비에서 고장" not in "설비에서 고장이 났습니다.")  # False

print(" " in "설비 고장 발생")  # True
# 따옴표로 감싼 공백(스페이스바)는 정말 "한 글자"로 취급

# ======================================
print("=== count() ===")

# .count() - 문자열에 특정 글자의 수(int)를 반환
# 문자열.count("찾을 글자")
print("banana".count("a"))  # 3
print("010-1234-1234".count("-"))  # 2
print("layla@spreatics.com".count("@"))  # 1


# ======================================
print("=== 실습 count()로 개수 세기 ===")
# 목표 : coun()로 특정 글자가 몇 번 나오는지 세기
# 단계 :
# "a,b,c,d" 저장
# count(",")로 쉼표 개수 출력

print("a, b, c, d".count(","))
